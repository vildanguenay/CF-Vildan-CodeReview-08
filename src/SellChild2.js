import React, {Component} from 'react';

class SellChild2 extends Component {
    render() {
      return (
                <p>{this.props.description}</p>
      );
    }
  }

export default SellChild2;